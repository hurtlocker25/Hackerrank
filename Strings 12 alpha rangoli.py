import string
def print_rangoli(size):
    alpha = string.ascii_lowercase
    temp = size*4-3 # total number of characters
    for i in range(size-1,-size):
        #temp = "-".join(alpha[i:size])
        rowvalue = "-"*temp

        for j in range()

        print(temp[::-1])
